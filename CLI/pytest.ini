[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
markers =
    integration: marks integration tests that need a mock Unity server
    slow: marks tests as slow (skip with -m "not slow")
